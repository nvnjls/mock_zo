import React from "react";

const Navbar = () => {
    return (
        <div className="fixed top-2 left-2 right-2 h-12 bg-gray-100 shadow-lg rounded-lg z-50">
        </div>
    );
};

export default Navbar;